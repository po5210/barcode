Ext.define('Bar.store.Trade', {
	
	extend : 'Ext.data.Store',
	
	requires: 'Bar.model.Trade',
	
	model : 'Bar.model.Trade',
	
	autoLoad : false,
	
	remoteFilter : false,
	
	remoteSort : false,
	
	pageSize : 50,
	
	proxy: {
		type: 'rest',
		url : '/domains/' + login.current_domain_id + '/trades',
		format : 'json',
	    reader: {
			type: 'json',
			root: 'items',
			successProperty : 'success',
			totalProperty : 'total'
        },
        writer: {
			type: 'json'
        }
	}
	
});