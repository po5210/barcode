Ext.define('Bar.store.Supplier', {
	
	extend : 'Ext.data.Store',
	
	requires: 'Bar.model.Supplier',
	
	model : 'Bar.model.Supplier',
	
	autoLoad : false,
	
	remoteFilter : false,
	
	remoteSort : false,
	
	pageSize : 50,
	
	proxy: {
		type: 'rest',
		url : '/domains/' + login.current_domain_id + '/suppliers',
		format : 'json',
	    reader: {
			type: 'json',
			root: 'items',
			successProperty : 'success',
			totalProperty : 'total'
        },
        writer: {
			type: 'json'
        }
	}
	
});